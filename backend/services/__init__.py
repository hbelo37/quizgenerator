"""
Service layer package.

- `langextract` provides the LangExtract-style text extraction interface.
- `llm_service` wraps calls to a local LLM (Ollama / HuggingFace).
- `quiz_service` orchestrates quiz generation and grading.
"""

