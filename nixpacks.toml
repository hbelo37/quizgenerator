# Nixpacks configuration (alternative to Dockerfile)
[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "cd backend",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "sh -c 'cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT'"
